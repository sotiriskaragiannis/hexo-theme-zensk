<main>
    <% if (is_post()) { %>
    <h1>
        <%= page.title %>
    </h1>
    <% } %>
    <p>
        <i><time><%= date(page.date, 'YYYY/MM/DD HH:mm:ss' ) %></time></i>
    </p>
    <%- page.content %>
</main>